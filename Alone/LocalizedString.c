#include "LocalizedString.h"
/*
LocalizedString* createLocalizedString(const char* locale, const char* str) {
	LocalizedString* stra = malloc(sizeof(LocalizedString));
	if(stra == NULL)
		return stra;
	stra->locale = malloc( strlen(locale) );
	if (stra->locale == NULL)
		return stra;
	stra->str = malloc(strlen(str));
	if (stra->str == NULL)
		return stra;
	return stra;
}*/